# Optional — only needed when you want PostgreSQL persistence.
# Copy this file to .env and replace YOUR_POSTGRES_PASSWORD with your actual password.
#
# If you skip this, the app runs fine for Phase 1/2 (job analysis works without DB).

# Google sign-in (Chrome extension)
# Create an OAuth client ID (type: Chrome extension OR Web application).
# Redirect URI must be: https://<YOUR_EXTENSION_ID>.chromiumapp.org/
GOOGLE_CLIENT_ID=758501915824-n03vtotgp025jc2gmn7hnsg5gh6lik0k.apps.googleusercontent.com
