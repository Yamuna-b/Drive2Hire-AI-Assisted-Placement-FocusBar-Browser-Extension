# Placement FocusBar

This repository contains the code for the Placement FocusBar project, including:

- Chrome extension (Manifest V3) with a side panel UI
- FastAPI backend (Python) with PostgreSQL integration
- Optional React web app scaffold (Vite)

See the `docs/` folder for detailed documentation.
